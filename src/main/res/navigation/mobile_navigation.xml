<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
            xmlns:app="http://schemas.android.com/apk/res-auto"
            xmlns:tools="http://schemas.android.com/tools"
            android:id="@+id/mobile_navigation"
            app:startDestination="@+id/nav_search">

    <fragment
            android:id="@+id/nav_search"
            android:name="com.agroneo.treeplace.specimens.SpecimensFragment"
            android:label="@string/menu_home"
            tools:layout="@layout/specimens_list"/>

    <fragment
            android:id="@+id/nav_creation"
            android:name="com.agroneo.treeplace.creation.CreationFragment"
            android:label="@string/menu_gallery"
            tools:layout="@layout/fragment_creation"/>

</navigation>